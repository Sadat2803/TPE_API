const {validateCreateTransporter} = require('./validateCreateTransporter')
const {validateDeleteTransporter} = require('./validateDeleteTransporter')
const {validateGetTransporter} = require('./validateGetTransporter')
const {validateUpdateTransporter} = require('./validateUpdateTransporter')

module.exports = {
    
    validateCreateTransporter,
    validateDeleteTransporter,
    validateGetTransporter,
    validateUpdateTransporter
}