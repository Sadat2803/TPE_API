const {validateCreateClient} = require('./validateCreateClient')
const {validateDeleteClient} = require('./validateDeleteClient')
const {validateGetClient} = require('./validateGetClient')
const {validateUpdateClient} = require('./validateUpdateClient')

module.exports = {
    
    validateCreateClient,
    validateDeleteClient,
    validateGetClient,
    validateUpdateClient
}